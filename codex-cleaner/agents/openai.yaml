interface:
  display_name: "Codex Cleaner"
  short_description: "Fail-closed Codex cleanup on Windows, macOS, Linux"
  default_prompt: "Use $codex-cleaner to run a fail-closed cross-platform storage audit, then require an expiring exact manifest and revalidation before any change."
